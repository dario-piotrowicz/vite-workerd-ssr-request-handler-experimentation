# Vite workerd ssr request handler experimentation

This repository shows an experimentation which consists in taking a preact application
and modifying its vite plugin so that it can perform server side rendering in the Cloudflare
workerd runtime.

## How to run it

Install the dependencies:
```
pnpm i
```

then you can run the preact app with:
```
pnpm preact:dev
```
